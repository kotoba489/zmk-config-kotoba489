# 左右分割機能を有効化
CONFIG_ZMK_SPLIT=y

# 左手を親機（Central）に設定
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

# 左右の通信強度を最大にする（これで見つかりやすくなります）
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# USB接続時でもBluetoothの広告（募集）を止めない設定
CONFIG_ZMK_USB=y
CONFIG_ZMK_BLE=y
